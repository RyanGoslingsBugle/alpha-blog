== Alpha-Blog

Simple blog template for Complete Ruby on Rails Developer course.

Not intended for production use, or for any use other than educational for that matter.

=== Install

To install:

* Clone the repo
* Run `bundle install`
* Setup the db `rake db:drop db:create db:migrate`
* Run the server `rails s --binding=YOUR_IP_ADDRESS`
